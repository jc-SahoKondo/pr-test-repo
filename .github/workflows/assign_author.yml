# PR作成時に、自動でAssigneeを設定

on:
  pull_request:
    types: [opened]
name: Assign author
jobs:
  assignAuthor:
    name: Assign author to PR
    runs-on: ubuntu-latest
    steps:
      - name: Assign author to PR
        uses: technote-space/assign-author@v1
